version: "3.2"

services:
  db:
    image: postgres:12-alpine
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root

  api:
    build:
      context: .
      dockerfile: ./api/Dockerfile
      cache_from:
        - docker.pkg.github.com/lucaspickering/gdlk/gdlk-api:${DOCKER_TAG:-latest}
    image: docker.pkg.github.com/lucaspickering/gdlk/gdlk-api:${DOCKER_TAG:-latest}
    volumes:
      - ./:/app:rw
    depends_on:
      - db
    environment:
      RUST_BACKTRACE: 1
      DATABASE_URL: postgres://root:root@db/gdlk

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
      cache_from:
        - docker.pkg.github.com/lucaspickering/gdlk/gdlk-frontend:${DOCKER_TAG:-latest}
    image: docker.pkg.github.com/lucaspickering/gdlk/gdlk-frontend:${DOCKER_TAG:-latest}
    volumes:
      - ./:/app:rw
